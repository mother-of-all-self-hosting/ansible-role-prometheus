# SPDX-FileCopyrightText: 2023 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Check existence of Prometheus systemd service
  ansible.builtin.stat:
    path: "{{ devture_systemd_docker_base_systemd_path }}/{{ prometheus_identifier }}.service"
  register: prometheus_service_stat

- name: Uninstall Prometheus systemd services and files
  when: prometheus_service_stat.stat.exists | bool
  block:
    - name: Ensure Prometheus systemd service is stopped
      ansible.builtin.service:
        name: "{{ prometheus_identifier }}"
        state: stopped
        enabled: false
        daemon_reload: true

    - name: Ensure Prometheus systemd service does not exist
      ansible.builtin.file:
        path: "{{ devture_systemd_docker_base_systemd_path }}/{{ prometheus_identifier }}.service"
        state: absent

    - name: Ensure Prometheus container network does not exist
      when: prometheus_container_network_deletion_enabled
      block:
        - name: Ensure Prometheus container network does not exist via community.docker.docker_network
          when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
          community.docker.docker_network:
            name: "{{ prometheus_container_network }}"
            state: absent
          register: prometheus_network_deletion_result

        - name: Ensure Prometheus container network does not exist via ansible.builtin.command
          when: devture_systemd_docker_base_container_network_creation_method == 'command'
          ansible.builtin.command:
            cmd: >-
              {{ devture_systemd_docker_base_host_command_docker }} network rm
              {{ prometheus_container_network }}
          register: prometheus_network_deletion_result
          changed_when: prometheus_container_network in prometheus_network_deletion_result.stdout

    - name: Ensure Prometheus path does not exist
      ansible.builtin.file:
        path: "{{ prometheus_base_path }}"
        state: absent
